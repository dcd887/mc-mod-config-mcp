# mc-mod-config-mcp 配置示例 — 复制为 .env 并按需填写（不填则用内置默认值）

# 支持扫描/解析的配置文件扩展名（逗号分隔，可扩展自定义扩展名，如加 properties）
# MOD_CONFIG_EXTENSIONS=cfg,toml,json

# 单个配置文件大小上限（MB），超过则跳过，防止误读超大/二进制文件
# MOD_CONFIG_MAX_FILE_MB=50

# 校验时字符串值长度告警阈值
# MOD_CONFIG_MAX_STRING_LEN=500
